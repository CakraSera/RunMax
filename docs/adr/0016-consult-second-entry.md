# Consult is a second entry, not home

Home stays the Board. ConsultSmith interviews, writes a Log, asks **Build this week?**, then hands off to existing Weeksmith `BuildThisWeek`. Consult never drafts Sessions. Empty Log on the Board CTA stays legal without Consult. Two traces: consult, then nested Build. Why: two doors, one fail-closed `saveWeek`. Chat as home and planner threads that draft Sessions stay out of scope.
