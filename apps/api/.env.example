# Server
PORT=8787
HOST=127.0.0.1

# Model provider (server-only; never VITE_*)
OPENAI_API_KEY=

# Auth token signing (JWT HS256) — openssl rand -hex 32
TOKEN_SECRET_KEY=

# PostgreSQL (Prisma) — docker compose db
DATABASE_URL=postgresql://runmax:runmax@localhost:15434/runmax
