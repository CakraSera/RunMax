# Notes

- Workspace lives in `document/learning/` (user request), not the repo root.
- Learner is a beginner. Analogies to `console.log` land; OTEL jargon does not.
- They already hit: traces missing in Langfuse after `runner:dev`; observation type not `Agent`; wanting `@anvia/langfuse` without app-level OTEL.
- Keep lessons short. Next topics if they ask: observation type `Agent`, env keys, UI filters vs ingest.
- Do not re-teach console.log. Use it as the known tool.
- New (mission shift): teach for a PRESENTATION. Level: "like a 7-year-old" — one analogy per concept, real term in parentheses. They will demo `runner:dev`, `studio`, `evals`, and the Langfuse UI, so numbers must match reality (2 agents, 8 tools, 13 evals, 6 notes).
