# Eval checking workflow delivered

Lesson 0005 taught the two-half report card: terminal PASS/FAIL from the pure-rule oracle, Langfuse traces as the per-fixture film. Includes the trace-name → suite mapping (BuildThisWeek ×10, ConsultThenBuild ×3, ConsultChat per chat turn) and the single-case fast loop (`evals <case-id>`).

**Implication:** the learner now has the full demo story (lessons 0002–0005). Before adding new lessons, drill recall: five steps of BuildThisWeek, then this eval flow. Natural next topics if they ask: Langfuse datasets/experiments (turning fixtures into a Langfuse dataset), LLM-as-judge, or CI blocking via `langfuse/experiment-action`.
