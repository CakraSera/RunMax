# Console.log is the known debug tool

The learner already debugs with `console.log`: print a value, look at the terminal, decide. They do not yet have an equivalent for tracing. They also already lived through a silent failure: `flush()` returned, Langfuse showed nothing. Future lessons should start from “print an id you can search,” not from OpenTelemetry architecture.
