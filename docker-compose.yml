services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: runmax
      POSTGRES_PASSWORD: runmax
      POSTGRES_DB: runmax
    ports:
      - "15434:5432" # 15433 is taken by another project's compose db on this host
